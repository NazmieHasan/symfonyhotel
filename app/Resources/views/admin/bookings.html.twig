{% extends 'base.html.twig' %}

{% block main %}

    <a class="btn btn-success" href="{{ path('admin_users')}}">VIEW ALL USERS</a>

    <h1>All Bookings:</h1>

    {% for booking in bookings %}
        <p><a href="{{ path('booking_view', { id: booking.id } ) }}">{{ booking.room}}</a></p>
    {% endfor %}

{% endblock %}

